/**
 * Since this is JS these types don't actually make a difference.
 * But they can be used to make it obvious what size int is expected.
 */
declare type Uint4 = number;
declare type Uint8 = number;
declare type Uint16 = number;
